<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Google Maps to load tiles -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.CustomApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToolShedd">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="" />

        <activity
            android:name=".screens.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.register.RegisterActivity" />
        <activity android:name=".screens.home.HomeActivity" />
        <activity android:name=".screens.profile.ProfileActivity" />
        <activity android:name=".screens.map.MapActivity" />
        <activity android:name=".screens.browse.BrowseActivity" />
        <activity android:name=".screens.addtool.AddToolActivity" />
        <activity android:name=".screens.tooldetail.ToolDetailActivity" />
    </application>

</manifest>